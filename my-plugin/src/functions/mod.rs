/* ============================
   FUNCTIONS MODULE INDEX
   - This file exposes all function modules inside `src/functions/`.
   - Add a `pub mod ...;` line for each new function file you create.
   - Re-export functions with `pub use ...::*;` so they will be
     imported from `crate::functions::*` in the dispatcher.
   ============================ */

pub mod example;
pub use example::*;